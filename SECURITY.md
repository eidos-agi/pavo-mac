# Security

- Plaud OAuth stays in `~/.plaud/`. Never commit tokens.
- Mock audio is synthetic bytes, not recordings.
- Do not put media under `artifacts/`.
- Human speaker identity is never approved by this tool.
